<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fom.boot.domain.meal.model.mapper.MealMapper">

    <!-- 식단 저장 -->
    <insert id="insertMealPlan" parameterType="com.fom.boot.domain.meal.model.vo.MealPlan">
        INSERT INTO MEAL_PLAN (
            PLAN_ID,
            MEMBER_ID,
            PLAN_NAME,
            SERVING_SIZE,
            TOTAL_COST,
            CALORIES,
            CARBS_G,
            PROTEIN_G,
            FAT_G,
            AI_RECIPE,
            WHEN_EAT,
            INGREDIENTS_JSON
        ) VALUES (
            SEQ_MEAL_PLAN.NEXTVAL,
            #{memberId},
            #{planName},
            #{servingSize},
            #{totalCost},
            #{calories},
            #{carbsG},
            #{proteinG},
            #{fatG},
            #{aiRecipe},
            #{whenEat},
            #{ingredientsJson}
        )
    </insert>

</mapper>
